# Django Settings
SECRET_KEY=
DEBUG=False

# Database (Django relational backend)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# MongoDB Atlas (for migration/auxiliary services)
MONGO_URI=
MONGO_DB_NAME=

# Pusher
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=
PUSHER_SSL=True

# JWT Configuration
JWT_SECRET=

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Email Settings
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USE_TLS=True
SMTP_EMAIL=
SMTP_PASSWORD=
DEFAULT_FROM_EMAIL=
